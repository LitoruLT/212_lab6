#include<stdio.h>
#include<string.h>
#include<ctype.h>

void main()
{
    int i,j,k,n,last,posi,posj;
    char command[260];
    scanf("%d",&n);
    scanf("%s",command);



    posi=0;posj=0;
    for(i=0 ; i<n ; i++)
    {
        for(j=0 ; j<n ; j++)
        {
            for(k=0 ; command[k]!='\0' ; k++)
            {
                switch(command[k])
                {
                case 'D':

                }
            }
        }
    }
}
